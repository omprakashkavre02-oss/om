The trained Linear Regression model is generated by running:

```bash
python src/train_model.py
```

This keeps the repository reproducible and avoids depending on a machine-specific serialized model.